_format_version: "3.0"
_transform: true

services:
  - name: service1
    host: service1
    port: 8080
    protocol: http
    routes:
      - name: service1
        paths:
          - /service1
        methods:
          - GET
          - POST

  - name: service2
    host: service2
    port: 8081
    protocol: http
    routes:
      - name: service2
        paths:
          - /service2
        methods:
          - GET
          - POST

